# platform - app service unit

Owned by **platform**. Reads its VPC and subnets from the network unit at
`../network`.

Generated by `terragrunt scaffold`. To change what a newly scaffolded unit looks like, edit
`modules/app-service/.boilerplate/` rather than editing units after the fact.

## Commands

```bash
terragrunt plan
terragrunt apply
terragrunt output
```
