# {{ .TeamName }} - app service unit

Owned by **{{ .TeamName }}**. Reads its VPC and subnets from the network unit at
`{{ .DependencyPath }}`.

Generated by `terragrunt scaffold`. To change what a newly scaffolded unit looks like, edit
`modules/app-service/.boilerplate/` rather than editing units after the fact.

## Commands

```bash
terragrunt plan
terragrunt apply
terragrunt output
```
